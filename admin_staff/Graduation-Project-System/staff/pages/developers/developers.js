document.addEventListener("DOMContentLoaded", function () {
    console.log("Staff Developers page ready.");
    // Logout is now handled generically by shared/js/layout.js for every
    // page's sidebar "Logout" link — no page-specific handler needed here.
});
